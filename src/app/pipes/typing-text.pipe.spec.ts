import { TypingTextPipe } from './typing-text.pipe';

describe('TypingTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TypingTextPipe();
    expect(pipe).toBeTruthy();
  });
});

import { TypingTextDirective } from './typing-text.directive';

describe('TypingTextDirective', () => {
  it('should create an instance', () => {
    const directive = new TypingTextDirective();
    expect(directive).toBeTruthy();
  });
});
